# Cost calculator addon
Lists an expanded list of usage statistics on the _Var dump_ page, including
total number of prompts and processing time/cost per user groups.

## Installation
Open `var_dump.php` in any text editor and change the configuration values to
your preferences. Save it and move the file to the `addons` folder on your
server.
