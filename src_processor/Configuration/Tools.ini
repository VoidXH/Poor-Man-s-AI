; Type of tool to create (also the class name in the Processor project)
[Weather]
; Unique ID for the tool the LLM can select
Name=get_weather
; Description to provide for the LLM about the tool
Description=Get the current weather in a given location
; Name of the first property (new properties can be added as Property2, Property3, etc.)
Property1=location
; Provide the LLM what to put in the result for this property
Property1Description=e.g. Santiago,Chile or Budapest
; Name of the second property
Property2=unit
; When Enum is used instead of Description, the LLM must choose one of the provided options
Property2Enum=celsius, fahrenheit
Property3=language
Property3Description=e.g. en or hu
; This property is required, others are optional
Required=location

; When the model has issues about auto tool usage and wants to use tools for everything,
; fix it by adding a tool for providing replies - do that by removing these comments.
;[Reply]
;Name=reply
;Description=Answer a general query
;Property1=reply
;Property1Description=Reply to user query
;Required=reply

[Sketch]
Name=sketch
Description=Draw/sketch things quickly, using SVG code
Property1=sketch
Property1Description=SVG code for what the user wanted to draw
Property2=prefix
Property2Description=Write text above the image (optional)
Property3=suffix
Property3Description=Write text below the image (optional)
Required=sketch

[HtmlOutput]
Name=html
Description=Output stylized responses using HTML formatting
Property1=html
Property1Description=Response in HTML format
Required=html

[Draw]
Name=draw
Description=Generate a detailed image (slower)
Property1=image_prompt
Property1Description=Comma separated list of keywords of each property of the image (subjects, background, colors, style, etc.)
Property2=prefix
Property2Description=Write text above the image (optional)
Property3=suffix
Property3Description=Write text below the image (optional)
Required=image_prompt
